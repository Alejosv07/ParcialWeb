@model IEnumerable<WebApplication3.Models.Detallepedido>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Disco.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pedido.Direccionentrega)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Precioventa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Disco.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pedido.Direccionentrega)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Precioventa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Iddetalle }) |
            @Html.ActionLink("Details", "Details", new { id=item.Iddetalle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Iddetalle })
        </td>
    </tr>
}

</table>
